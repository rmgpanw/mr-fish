source("renv/activate.R")

options(repos = c(CRAN = "https://packagemanager.posit.co/cran/latest",
                  cloudr = "https://cloud.r-project.org",
                  mrcieu = "https://mrcieu.r-universe.dev"))

# additional dependencies
if (FALSE) {
  library(visNetwork)
}
